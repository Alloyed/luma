dependencies:
    pre:
        - sudo apt-get update; sudo apt-get install lua5.1 luarocks
        - sudo luarocks install busted
    post:
        - sudo luarocks make
test:
    override:
        - mkdir -p $CIRCLE_TEST_REPORTS/junit
        - busted -o junit > $CIRCLE_TEST_REPORTS/junit/tests.xml
